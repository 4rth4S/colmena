# Colmena Backlog — fecha: 2026-05-07
# Formato: lista de problemas/features con dependencias, esfuerzo estimado,
# y agrupación en waves para ejecución paralela (dark factory).
#
# Cada entry tiene:
#   id:         slug único
#   title:      una línea descriptiva
#   what:       qué hay que hacer (conciso, machine-readable)
#   why:        motivación (una frase)
#   effort:     S (<2h) / M (2-8h) / L (8-24h) / XL (>24h)
#   risk:       low / medium / high (probabilidad de romper algo)
#   blocks:     [ids que esto bloquea]
#   blocked_by: [ids que bloquean esto]
#   wave:       grupo de paralelismo sugerido (0 = ya, 1 = primera wave, etc.)
#   status:     pending / in_progress / done

problems:
  # ── Wave 0 — Cleanup inmediato (< 1 sesión) ──

  - id: m7.14-followups
    title: "M7.14 post-merge followups — DONE (4 code + 2 doc)"
    what: |
      1. queue.rs: gate old_suffix on session_id absence ✓ (7956834)
      2. config.rs: remove eprintln! hot-path spam ✓ (7956834)
      3. queue.rs: clarify legacy prune_old_entries ✓ (7956834)
      4. cli/main.rs: u64 for older_than_days ✓ (7956834)
      5-6. Doc items resolved during docs-regeneration PR #65
    why: "Surfaced by code_reviewer + auditor durante PR #52."
    effort: S
    risk: low
    blocks: []
    blocked_by: []
    wave: 0
    status: done
    done_in: 7956834 + PR #65
    tags: [bug, chore, cleanup]
    produces_pr: true

  - id: merge-autonomy
    title: "Auto-merge para PRs de library/docs/chore + mergear yo mismo"
    what: |
      1. Mover gh pr merge de blocked → restricted con bash_pattern scoped
      2. bash_pattern permite merge en branches feature/library-*, docs/*, chore/*
      3. Regla: CI verde es precondición (verifico antes de mergear)
      4. branches de código (feature/*, fix/*) siguen requiriendo Coco
    why: "Dark factory necesita merge autónomo para fluir entre waves."
    effort: S
    risk: low
    blocks: [dark-factory-coordination]
    blocked_by: []
    wave: 0
    status: pending
    tags: [feature, firewall, devops]
    produces_pr: true

  - id: auto-elevate-skeleton
    title: "Auto-elevate por esqueleto de binario — 2× Y → delegación session-scoped"
    what: |
      Cuando evaluate_chain_aware (M7.10) retorna ask para una pieza de una cadena:
      1. Extraer el esqueleto: el binario base + flag principal si aplica.
         curl -I https://api.coinbase.com/v2/accounts/123 → esqueleto: curl
         git diff colmena-core/src/firewall.rs        → esqueleto: git diff
         cargo test -p colmena-core                   → esqueleto: cargo test
         tee test.log                                 → esqueleto: tee
         head -20                                     → esqueleto: head
      2. Si mismo (agent_type, skeleton) recibe Y del operador 2× en ventana 10min:
         → auto-crear delegación session-scoped con bash_pattern: '^skeleton\b'
      3. Delegación: session-scoped, TTL 1h (max 8h), reason "auto-elevated:2x-confirm"
      4. Audit log registra la elevación
      5. Si operador responde N → reset counter para ese skeleton
      6. Kill switch: trust-firewall.yaml auto_elevate: false
      7. NO aplica a comandos en blocked (nunca se auto-eleva rm -rf, gh pr merge, etc.)
    why: |
      Hash exacto del comando no funciona en práctica: curl api/v2/accounts/123
      y curl api/v2/accounts/456 tienen distinto hash pero son "mismo comando".
      El esqueleto captura la primitiva bash real que el operador está aprobando.
      M7.10 ya hace el split de cadenas — esto capitaliza ese trabajo.
    effort: M
    risk: medium
    blocks: []
    blocked_by: []
    wave: 0
    status: pending
    tags: [feature, ux, firewall, chain-aware]
    produces_pr: true
    memory_ref: project_firewall_auto_elevate_2x_yes.md

  # ── Wave 1 — Independientes entre sí ──

  - id: m7.11-session-end-banner
    title: "M7.11 SessionEnd value banner — stats al cerrar sesión"
    what: |
      1. Nuevo handler en colmena-cli para evento Stop/SessionEnd
      2. Lee audit.log + filter-stats.jsonl para la sesión actual
      3. Imprime banner a stderr con prompts-saved + tokens-saved
      4. Safe fallback: cualquier error → sin output, zero impacto en /exit
      5. Registrar hook en install.rs + setup.rs
    why: "Cada sesión cierra con prueba de valor. No depende de que el assistant recuerde."
    effort: M
    risk: low
    blocks: []
    blocked_by: []
    wave: 1
    status: pending
    tags: [feature, marketing, hooks]
    produces_pr: true
    memory_ref: project_m711_session_end_value_banner.md

  - id: m7.4-role-ergonomics
    title: "M7.4 Role ergonomics CLI — DONE (clone, doctor, bundled fixes)"
    what: |
      1. colmena library clone-role <existing> --as <new_id> ✓
      2. colmena library doctor <id> — validar YAML + sugerir mejoras ✓
      3. colmena library create-role — creación con intelligent defaults ✓
      4. Bundled fixes:
         a. colmena suggest agent count inflation fix ✓
         b. library select CLI manejo de EOF ✓
         c. delegate add --bash-pattern / --path-within flags ✓
         d. mission_spawn scope via manifest --scope ✓
      Nota: 'inherit' se subsume en clone-role (copiar como template + modificar).
    why: "Pedido explícito de los 4 power users. Mayor fricción reportada."
    effort: L
    risk: medium
    blocks: []
    blocked_by: []
    wave: 1
    status: done
    done_in: PR #61
    tags: [feature, ux, cli]
    produces_pr: true
    memory_ref: project_m712_state.md

  - id: m7.13-crates-io
    title: "M7.13 crates.io publication — DONE"
    what: |
      1. Package rename: colmena-cli → colmena ✓
      2. Nombres verificados free 2026-04-23 ✓
      3. Path-dep versions agregadas ✓
      4. CARGO_REGISTRY_TOKEN configurado ✓
      5. Publicado: core → filter → mcp → cli (v0.14.0 / v0.14.2) ✓
      6. Release automation en release.yml ✓
    why: "Distribución fuera de git clone."
    effort: M
    risk: medium
    blocks: []
    blocked_by: []
    wave: 1
    status: done
    done_in: PR #59 + PR #60
    tags: [feature, release, distribution]
    produces_pr: true

  # ── Wave 2 — Dependen de Wave 1 o entre sí ──

  - id: m7.3.1-anti-reciprocal-fix
    title: "M7.3.1 Anti-reciprocal invariant — scoped per-mission → FIXED (M7.3.2)"
    what: |
      El anti-reciprocal filter fue REMOVIDO en M7.3.2.
      Cross-mission reviewer reuse ahora está explícitamente permitido.
      review.rs:121-127 documenta el cambio. Test test_submit_cross_mission_reviewer_reuse_allowed lo verifica.
      Perspective diversification across missions → M7.7 (future).
    why: "Bloqueaba reutilización legítima de reviewer entre misiones."
    effort: S
    risk: low
    blocks: []
    blocked_by: []
    wave: 2
    status: done
    done_in: M7.3.2 (post dogfood 2026-04-21)
    tags: [bug, review, elo]
    produces_pr: true
    memory_ref: project_review_reciprocal_cross_mission_bug.md

  - id: m7.3-description-omission
    title: "M7.3 mission_spawn omite description en subagent files — FIXED"
    what: |
      Auto-generated subagent files ya incluyen description en frontmatter.
      write_subagent_file() recibe description del role YAML.
      Verificado: mission-lead subagent file tiene description correcta.
    why: "ELO cycle se rompía si CC omitía agentes sin description."
    effort: S
    risk: low
    blocks: []
    blocked_by: []
    wave: 2
    status: done
    done_in: M7.3 fix (post dogfood 2026-04-21)
    tags: [bug, mission-spawn, elo]
    produces_pr: true
    memory_ref: project_mission_spawn_missing_description.md

  - id: m7.3-scope-gap
    title: "M7.3 mission_spawn scope gap — FIXED (M7.12 workspace_scope)"
    what: |
      Pattern.workspace_scope: repo-wide resuelve el scope gap para misiones de refactor.
      spawn_mission() aplica apply_repo_wide_scope() cuando el pattern lo declara.
      Delegate add --path-within disponible para scope manual.
      Secret exclusions por default: *.env, *credentials*, *secret*, *.key, *.pem.
    why: "Misiones de refactor necesitan acceso al repo root."
    effort: S
    risk: low
    blocks: []
    blocked_by: []
    wave: 2
    status: done
    done_in: M7.12 (PR #39)
    tags: [bug, mission-spawn, scope]
    produces_pr: true
    memory_ref: project_mission_spawn_scope_gap.md

  - id: m7.15-mission-manifest
    title: "M7.15 Mission Manifest — DONE (multi-PR)"
    what: |
      PR #62 (m7.15a): MissionManifest struct, validation, CLI validate + init
      PR #63 (m7.15b): mission_spawn --from <manifest>, MCP manifest_path, mission_abort,
                       runtime-agent-overrides.json, mission init --from-history
      PR #64: multi-instance fan-out con namespaced agent_ids
      PR #66: --auto-spawn con Mission Lead role (último 20% de automatización)
      Queda pendiente: acceptance criteria mecanizado (M7.16)
    why: "Dark factory: lanzar squads desde un manifiesto. 4 PRs merged."
    effort: XL
    risk: high
    blocks: [dark-factory-coordination]
    blocked_by: []  # todos los blockers resueltos
    wave: 2
    status: done
    done_in: PRs #62 + #63 + #64 + #66
    tags: [feature, mission-spawn, architecture]
    produces_pr: true
    memory_ref: docs/architect/M7-15-mission-manifest/

  # ── Wave 3 — Features que dependen de wave 2 ──

  - id: dark-factory-coordination
    title: "Dark factory coordination — multi-session parallelism con git merge gate"
    what: |
      1. Documentar el flujo: operator escribe manifiesto → N sesiones CC en paralelo
      2. Cada sesión produce un PR contra main
      3. PRs de library/docs se auto-mergean; PRs de código requieren Coco
      4. Entre waves: merge secuencial → cargo test --workspace → next wave
      5. Tooling: script que lance N sesiones CC con prompts independientes
      (Futuro M7.16+: orquestador automático que lee backlog.yaml y dispara waves)
      M7.15 (mission manifest + auto-spawn) ya destrabó el blocker principal.
    why: "Llevar el dark factory concept a operación real. v1 manual, v2 automatizado."
    effort: M
    risk: low
    blocks: []
    blocked_by: [merge-autonomy]  # único blocker restante
    wave: 3
    status: pending
    tags: [feature, devops, orchestration]
    produces_pr: false  # doc + tooling, no código en el repo

  # ── Futuro (post wave-3, no planeado aún) ──

  - id: m7.7-multi-perspective-reviewers
    title: "M7.7 Multi-perspective reviewer diversification"
    what: |
      Reviewer selection con complementariedad de categorías ELO, no random puro.
      Evitar same-tribe approval. Exponer colmena review perspectives <mission>.
    why: "Near-future need flagged by Coco. ELO events deben reflejar cross-viewpoint judgement."
    effort: L
    risk: medium
    blocks: []
    blocked_by: []
    wave: future
    status: pending
    tags: [feature, review, elo]
    produces_pr: true

  - id: m7.6-install-mode-b
    title: "M7.6 Install Mode B como first-class — DONE"
    what: |
      docs/install-mode-b.md documenta el onboarding donde CC del usuario lee el repo y se autoconfigura.
      README referencia Install Mode B. Validado con 4 power users.
    why: "Validado con 4 power users. Más rápido que leer docs."
    effort: S
    risk: low
    blocks: []
    blocked_by: []
    wave: future
    status: done
    done_in: PR #65 (docs-regeneration)
    tags: [docs, onboarding]
    produces_pr: true

  - id: colmena.space-landing
    title: "Landing page colmena.space — frontend + use-case cards"
    what: |
      Domain comprado. 4 use-case cards propuestas. Frontend + presentación + docs linking.
    why: "First-time visitor feedback: diseño gustó, pidieron use-case cards."
    effort: M
    risk: low
    blocks: []
    blocked_by: []
    wave: future
    status: pending
    tags: [feature, frontend, marketing]
    produces_pr: true

  # ── Métricas de referencia ──

metrics:
  total_problems: 14
  done: 8           # m7.13, m7.15, m7.3.1, m7.3-desc, m7.3-scope, m7.4, m7.6, m7.14-followups
  pending: 4         # merge-autonomy, auto-elevate, m7.11, dark-factory
  future: 2          # m7.7, landing
  total_prs_estimated: 4  # solo los pending

  effort_summary:
    done_S: 4   # m7.14-followups, m7.3.1, m7.3-description, m7.3-scope-gap
    done_M: 2   # m7.13, m7.6
    done_L: 1   # m7.4
    done_XL: 1  # m7.15
    pending_S: 1   # merge-autonomy
    pending_M: 3   # auto-elevate, m7.11, dark-factory
    pending_L: 0
    pending_XL: 0

  # Waves actualizado:
  # Wave 0 (ahora):    [merge-autonomy] [auto-elevate]                       → 2 PRs, paralelo
  # Wave 1 (ahora):    [m7.11-session-banner]                                → 1 PR
  # Wave 2 (dep w0):   [dark-factory-coordination]                           → docs + tooling
  # Future:             [m7.7] [colmena.space-landing]
